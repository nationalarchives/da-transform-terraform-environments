version: 0.2

phases:
  pre_build:
    commands:
      - aws --version
  build:
    commands:
      - cd codepipeline/lambda-deploy
      - pip install -r requirements.txt
      - |
        ./update_step_functions.sh \
          "${SF_TF_VAR_KEY_LIST}" \
          "dte-96-tfvars" \
          "../../lambda_functions"
