version: 0.2

phases:
  build:
    commands:
       - aws --version
       - echo TESTING PARSER
       - cd ${CODEBUILD_SRC_DIR_teDockerBuild}
       - cd testing/parser_testing
       - python3 test_parser_lambda_fn.py
