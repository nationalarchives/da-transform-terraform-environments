version: 0.2

phases:
  pre_build:
    commands:
       - aws --version
  build:
    commands:
       - cd codepipeline/lambda-deploy
       - pip install -r requirements.txt
       - ./deploy_images_to_ecr.sh "$AWS_REGION"
